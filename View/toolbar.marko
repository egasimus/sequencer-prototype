.Toolbar
  button class=(input.playing && "active")
    if(input.playing)
      -- <strong>P</strong>AUSE
    else
      -- <strong>P</strong>LAY
  button class=(input.looping && "active")
    -- <strong>L</strong>OOP
  button -- <strong>U</strong>NDO
  button -- <strong>R</strong>EDO
  .Spacer
  button -- <strong>M</strong>ARK (${input.markers.length})
  button -- <strong>A</strong>PPEND
  button -- <strong>O</strong>VERDUB
